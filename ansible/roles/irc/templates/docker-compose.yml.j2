# {{ ansible_managed }}
---
version: '2'
services:
  thelounge:
    image: thelounge/thelounge:latest
    container_name: thelounge
    ports:
      - {{ thelounge_port }}:9000
    restart: always
    volumes:
      - {{ docker_home_dir }}/thelounge/config:/var/opt/thelounge


